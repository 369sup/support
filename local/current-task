# Current task

## Objective

- 建立/補齊「AGENTS.md」與「README.md」的啟動互相引用機制，讓 Codex 5.3 Spark 在此 repo 起步時可快速到達權威指令來源，並降低啟動時重複搜尋上下文。
- 範圍為 repo 內所有 tracked 的 `AGENTS.md` 與 `README.md`（目前刻意排除未追蹤的 vendor/cache 路徑）。

## Scope

- 變更範圍：
  - `.codex/*`、`.github/*`、`.serena/*`
  - `AGENTS.md`、`apps/AGENTS.md`、`apps/web/**/AGENTS.md`
  - `docs/**`、`packages/**`、`plugins/**`、`scripts/**`
  - `apps/web/src/app/(public|resources)/**/README.md`
  - `apps/web/src/modules/**/README.md`
  - `README.md`（repo root）
- 明確不在本次變更內：
  - `.agents/*`（目前環境為唯讀）
  - `apps/web/node_modules/**`、`apps/web/.next/**`（非程式授權來源）

## Verified facts

- `git status --short --branch` 已確認有大量 AGENTS/README 檔案改動。
- `git ls-files` 與變更清單比對結果顯示目前 tracked 的 `AGENTS.md`/`README.md`，僅剩 `.agents/AGENTS.md`、`.agents/plugins/README.md`、`.agents/skills/README.md` 未變更（權限/作者性質不在 scope）。

## Assumptions

- `apps/web/node_modules`、`apps/web/.next` 下 AGENTS/README 來自套件與建置快取，雖可掃到但不應作為 repo 權威來源。

## Decisions

- 採取「文檔插入式」低風險方式：在每個 AGENTS 添加「先讀此 AGENTS，再接著讀該層 README」提示；在每個 README 添加「先讀 AGENTS 鏈，再讀 README」提示。
- 不同意修改業務/路由程式碼。

## Completed

- 已補齊 24 個 `AGENTS.md`。
- 已補齊 113 個 `README.md`。
- 全部為同一語意的啟動順序提示，未調整行為性邏輯。
- 已將 `AGENTS.md` 主要放到 `D:\GitHub\support\` 的 `AGENTS.md` authority map 中作為非語音啟動路徑索引，並在 root+重要子樹 README 中補上對應導向。
- 已完成 `git status`、`git diff --check --`、`git ls-files`（篩選 tracked AGENTS/README）與變更集合一致性。

## Current state

- 任務完成度：主要文件已更新完成。
- 保留 `local/current-task` 作為持續追蹤。
- 已完成大規模文件互參照修正；待確認是否要進一步處理用戶列出的 vendor/cache 路徑（`node_modules`、`.next`）但這些目前非 Git 受控，建議在需求明確後再做「不建議」的補正。

## Validation

- `git status --short --branch`
- `git diff --check --`
- `git diff --name-only | Where-Object { -not ($_ -match '/AGENTS\.md$|/README\.md$') }`（僅剩非文件為 `local/current-task`）
- `git ls-files` 過濾確認 tracked AGENTS/README 的覆蓋情形
- 檢核 `git ls-files 'apps/web/src/app/**/*README.md'`、`git ls-files 'apps/web/src/modules/**/*README.md'`、`git ls-files '*/**/AGENTS.md'` 與 diff 集合一致。

## Risks

- `node_modules` 與 `.next` 內有大量 README/AGENTS 覆蓋，現階段均為未受版本控制內容；未納入權威鏈避免追蹤污染與啟動脈絡膨脹。
- `.agents` 權限/內容來源特性導致本輪未納入同步；不影響 repo tracked 的授權流程，但若要「逐字一致」需另行確認範圍。

<!-- serena-memory-candidates:start -->
```json
{
  "schemaVersion": 1,
  "checkpointToken": "019f9931-4330-7131-9ffe-cafde4116646",
  "disposition": "no-memory",
  "candidates": []
}
```
<!-- serena-memory-candidates:end -->
