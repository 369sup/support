# Current task

## Objective
保留唯一登入頁路由為 `apps/web/src/app/(public)/login/page.tsx`，並加入可用的 mock 帳號（帳號 `mock` / 密碼 `123456`）供開發登入使用。

## Scope
- `apps/web/src/app/login/page.tsx`
- `apps/web/src/app/(public)/login/page.tsx`
- `apps/web/src/modules/identity/authentication/adapters/inbound/react/development-sign-in-form.tsx`
- `apps/web/src/modules/identity/authentication/adapters/outbound/persistence/in-memory-development-credential.adapter.ts`
- 不修改其他功能範圍。

## Verified facts
- `apps/web/src/app/(public)/login/page.tsx` 已存在且為登入頁實作。
- `apps/web/src/app/login/page.tsx` 已刪除。
- Mock 帳號清單加入 `{ username: "mock", password: "123456" }`。
- 登入頁預設帳號改為 `mock`，預設密碼改為 `123456`。
- 執行 `pnpm.cmd --filter @support/web typecheck` 失敗（`oauth-authorizations` 相關既有 TS 參考錯誤）。
- 目前僅有三個使用者相關變更檔案；`(public)/login` 為未追蹤新增目錄/檔。

## Assumptions
- 使用者只要在 `?enable`/in-memory 開發模式可用時登入。
- 根目錄 `/login` 不再需要保留其他實作入口。

## Decisions
- 採用最小修正：將登入頁實際檔案移入 `(public)/login`，不動其他 login 流程邏輯。
- 僅調整開發環境預設填值與 mock 憑證，不改變認證 API。

## Completed
- 目錄 `apps/web/src/app/login/page.tsx` 刪除。
- 新增 `apps/web/src/app/(public)/login/page.tsx`（由原登入頁內容複製/移植）。
- 新增 mock 憑證 `mock / 123456`。
- 更新登入表單預設帳號、密碼與提示文字。

## Current state
- 功能變更已完成：唯一 login route 檔來源指向 `(public)/login`。
- 路由/build 未在此任務中確認全部通過（見 Validation）。

## Remaining
- 若要完成「完整驗證」，需修復既有 `apps/web/src/modules/integrations/oauth-authorizations/.../evaluate-oauth-authorization.use-case.ts` 的 TS 錯誤後，再重跑 `pnpm typecheck/build`。

## Validation
- `git status --short --branch`
- `Test-Path` 檢查 `apps/web/src/app/login` 路徑不存在
- `git diff --stat`（僅顯示登入相關 3 個變更）
- `pnpm.cmd --filter @support/web build`（目前失敗：`EvaluateOAuthAuthorizationInput/Result` 參考錯誤）
- `pnpm.cmd --filter @support/web typecheck`（同上既有錯誤）

## Risks
- `oauth-authorizations` 中的既有型別參考錯誤會阻擋 `pnpm ... typecheck/build`，需另行修正。
- `git` 有顯示 `CRLF/LF` 轉換警告，可能是作業環境換行設定造成。
