# Current task

## Objective
完成 S02（`organizations/organization-policies`）：使 OAuth / GitHub App 組織政策決策可落地為 active context，並修正相關架構與型別一致性，維持最小變更。

## Scope
本輪僅修改：
- `apps/web/src/modules/organizations/organization-policies`
- `apps/web/src/modules/repositories/repository-access/README.md`
- `docs/architecture/module-map.json`、`docs/architecture/module-map.md`
- `scripts/architecture.test.mjs`
- `.serena/memories/shared/*.md`（已由已授權架構更新流程同步）

排除：
- `apps/web/src/app` 路由與 API 契約
- 外部 OAuth/GitHub App 以外的整體整合流程

## Verified facts
- `pnpm.cmd check` 已在完整流程中通過（architecture required/generated、test:architecture、test:memory、lint/typecheck/test）。
- `organization-policies` 現已在 `docs/architecture/module-map.json` 與 `module-map.md` 標示為 `implementationStatus: "active"`、`semanticStatus: "validated"`，並新增 `activationScope`/`semanticClaims`。
- `apps/web/src/modules/organizations/organization-policies` 的核心型別與 handler 已改為直接使用 `ResolveAppAccessDecisionResult`（避免 `AppAccessPolicyDecision` 具名別名推斷不一致）。
- 為符合 `ARCH-API-005` 公共入口點規則，`server-api.ts` 現改以 `composition` facade 方式導出 `resolveAppAccessDecision`。

## Assumptions
- 選擇不擴張到全域 `integrations/github-app-installations` 流程；僅完成缺口卡片最小落地邊界。
- `organization-policies` 目前以 in-memory seed 來源為前提，無需引入新持久化基礎設施。

## Decisions
- 移除 domain 層的預設 builder 函式，改在 handler 內部直接回傳 inline fallback policy 物件以保留最小實作。
- 透過 `composition/organization-policies.composition.ts` 封裝 facade，讓 `server-api.ts` 回傳 `organizationPoliciesServerFacade.resolveAppAccessDecision`（符合公共入口點合規）。
- 將 `AppAccessPolicyDecision` 型別從 `server-api.ts`/handler 走 `./contracts/app-access-policy-decision` 匯出路徑，並保留 `integration-contracts.ts` 內既有整合合約轉接。

## Completed
- `apps/web/src/modules/organizations/organization-policies/application/ports/inbound/resolve-app-access-decision.use-case.ts`
- `apps/web/src/modules/organizations/organization-policies/application/queries/resolve-app-access-decision.handler.ts`
- `apps/web/src/modules/organizations/organization-policies/domain/organization-app-access-policy.ts`
- `apps/web/src/modules/organizations/organization-policies/tests/resolve-app-access-decision.test.ts`
- `apps/web/src/modules/organizations/organization-policies/server-api.ts`
- `apps/web/src/modules/organizations/organization-policies/composition/organization-policies.composition.ts`（新增）
- `apps/web/src/modules/organizations/organization-policies/README.md`
- `apps/web/src/modules/repositories/repository-access/README.md`（移除不符 schema 欄位）
- `docs/architecture/module-map.json`
- `docs/architecture/module-map.md`
- `scripts/architecture.test.mjs`

## Current state
- S02 已完成本輪預期交付，相關架構與 lint/typecheck/test 已通過。
- 仍待你決定是否要接著起下一張卡 S03，或先整理提交/提案。

## Remaining
- （本輪）無未完成 blocking 項；下一步如要可直接進行 S03（`github-app-user-authorization`）。

## Validation
- `git -C D:\GitHub\support status --short --branch`
- `pnpm.cmd check`（完整通過）
- `git -C D:\GitHub\support diff --check`（無 whitespace 錯誤）

## Risks
- `.serena/memories/shared/module-catalog.md` 與 `ownership.md` 仍有 auto-sync 變更，需在提交時一併評估是否採用。
- `server-api.ts` 已調整為 composition 入口模式；若未來需簡化 DI，可以再收斂為共用模式但不影響現況。
