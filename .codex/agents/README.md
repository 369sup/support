# Project custom agents

Optional project custom agents live here as `<agent-name>.toml`.

Each active file must define `name`, `description`, and
`developer_instructions`. Do not add an empty agent merely to preserve this
directory; this README is the inert placeholder. Follow [../AGENTS.md](../AGENTS.md).
