# Project-scoped Codex configuration.
#
# Intentionally contains no active settings. Codex loads this layer only when
# the repository is trusted. Add the smallest reviewed setting required by the
# whole project; keep personal preferences and credentials in ~/.codex.
