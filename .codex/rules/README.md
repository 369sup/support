# Project command rules

Optional experimental command-policy files live here as `<rule-name>.rules`.

No rule is active in the scaffold. Add only narrow reviewed prefixes with
justification plus positive and negative matching examples. Follow
[../AGENTS.md](../AGENTS.md).
