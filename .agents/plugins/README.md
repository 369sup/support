# Repository plugin marketplace

`marketplace.json` catalogs installable plugins whose source normally lives in
repository-root `plugins/<plugin-name>/`.

The initial catalog is intentionally empty. Add an entry only after its plugin
manifest, bundled skills, permissions, authentication policy, and paths have
been reviewed. Follow [../AGENTS.md](../AGENTS.md).
