import { renderUnavailableRoute } from "@/app/_route-contracts/route-contract";

export default function LatestRepositoryReleasePage(): never {
  return renderUnavailableRoute("page-owner-repository-releases-latest");
}
