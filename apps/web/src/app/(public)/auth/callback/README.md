<!-- Generated by `pnpm architecture:docs` from `apps/web/route-map.json`. Do not edit directly. -->

# Supabase Auth callback

## URL contract

- **Route ID:** `handler-auth-callback`
- **Support path:** `/auth/callback`
- **GitHub canonical patterns:** No official pattern is asserted.
- **Delivery:** `handler`
- **Status:** `active`
- **Materialization:** `active`

## Functional intent

Exchanges a PKCE code or verifies an email OTP before redirecting to the requested safe application path.

The filesystem route is active. Its business behavior remains owned by the referenced module contracts.

## Delivery functions

- `GET`: `GET` — Validates the provider callback and establishes the cookie-backed session.

## Module contracts

- - **owner:** `identity/authentication` — use cases: no route-level use-case reference; functions: `completeExternalSignIn`, `isSupabaseAuthenticationEnabled`, `verifySupabaseOtp`

The module README remains the semantic authority for each complete thirteen-field use-case contract.

## Parameters

### Path

None.

### Query

- - `code`: optional, single
- - `token_hash`: optional, single
- - `type`: optional, single
- - `next`: optional, single
- - `sb_flow_id`: optional, single

Query keys not declared here are rejected by the typed URL builder.

## Sources

- - `repository-app-router`: repository source `apps/web/src/app` — Support filesystem route materialization; delivery functions and module imports
