<!-- Generated by `pnpm architecture:docs` from `apps/web/route-map.json`. Do not edit directly. -->

# Reauthenticate Handler

## URL contract

- **Route ID:** `handler-api-development-auth-account-sessions-sessionid-reauthenticate`
- **Support path:** `/api/development/auth/account-sessions/{sessionId}/reauthenticate`
- **GitHub canonical patterns:** No official pattern is asserted.
- **Delivery:** `handler`
- **Status:** `active`
- **Materialization:** `active`

## Functional intent

Handles POST requests for `/api/development/auth/account-sessions/{sessionId}/reauthenticate` through `hasSameOrigin`, `isInMemoryRuntimeEnabled`, `readBrowserSessionToken`, `reauthenticateSession`.

The filesystem route is active. Its business behavior remains owned by the referenced module contracts.

## Delivery functions

- `POST`: `POST` — Processes the POST transport contract without moving business policy into the route.

## Module contracts

- - **owner:** `identity/authentication` — use cases: `reauthenticate-session`; functions: `hasSameOrigin`, `isInMemoryRuntimeEnabled`, `readBrowserSessionToken`, `reauthenticateSession`

The module README remains the semantic authority for each complete thirteen-field use-case contract.

## Parameters

### Path

- - `sessionId`: `scalar`

### Query

None.

Query keys not declared here are rejected by the typed URL builder.

## Sources

- - `repository-app-router`: repository source `apps/web/src/app` — Support filesystem route materialization; delivery functions and module imports
