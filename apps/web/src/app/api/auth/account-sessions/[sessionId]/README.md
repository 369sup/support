<!-- Generated by `pnpm architecture:docs` from `apps/web/route-map.json`. Do not edit directly. -->

# [Session Id] Handler

## URL contract

- **Route ID:** `handler-api-auth-account-sessions-sessionid`
- **Support path:** `/api/auth/account-sessions/{sessionId}`
- **GitHub canonical patterns:** No official pattern is asserted.
- **Delivery:** `handler`
- **Status:** `active`
- **Materialization:** `active`

## Functional intent

Handles DELETE requests for `/api/auth/account-sessions/{sessionId}` through `clearBrowserSessionToken`, `hasSameOrigin`, `isInMemoryRuntimeEnabled`, `readBrowserSessionToken`, `removeAccountSession`, `restoreLastValidDashboardContext`.

The filesystem route is active. Its business behavior remains owned by the referenced module contracts.

## Delivery functions

- `DELETE`: `DELETE` — Processes the DELETE transport contract without moving business policy into the route.

## Module contracts

- - **owner:** `projections/dashboard` — use cases: `restore-last-valid-dashboard-context`; functions: `restoreLastValidDashboardContext`
- - **collaborator:** `identity/authentication` — use cases: `remove-account-session`; functions: `clearBrowserSessionToken`, `hasSameOrigin`, `isInMemoryRuntimeEnabled`, `readBrowserSessionToken`, `removeAccountSession`

The module README remains the semantic authority for each complete thirteen-field use-case contract.

## Parameters

### Path

- - `sessionId`: `scalar`

### Query

None.

Query keys not declared here are rejected by the typed URL builder.

## Sources

- - `repository-app-router`: repository source `apps/web/src/app` — Support filesystem route materialization; delivery functions and module imports
