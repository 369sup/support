<!-- Generated by `pnpm architecture:docs` from `apps/web/route-map.json`. Do not edit directly. -->

# Account Sessions Handler

## URL contract

- **Route ID:** `handler-api-auth-account-sessions`
- **Support path:** `/api/auth/account-sessions`
- **GitHub canonical patterns:** No official pattern is asserted.
- **Delivery:** `handler`
- **Status:** `active`
- **Materialization:** `active`

## Functional intent

Handles DELETE, GET requests for `/api/auth/account-sessions` through `clearBrowserSessionToken`, `hasSameOrigin`, `isInMemoryRuntimeEnabled`, `listBrowserAccountSessions`, `readBrowserSessionToken`, `signOutAllSessions`.

The filesystem route is active. Its business behavior remains owned by the referenced module contracts.

## Delivery functions

- `DELETE`: `DELETE` — Processes the DELETE transport contract without moving business policy into the route.
- `GET`: `GET` — Processes the GET transport contract without moving business policy into the route.

## Module contracts

- - **owner:** `identity/authentication` — use cases: `list-browser-account-sessions`, `sign-out-all-sessions`; functions: `clearBrowserSessionToken`, `hasSameOrigin`, `isInMemoryRuntimeEnabled`, `listBrowserAccountSessions`, `readBrowserSessionToken`, `signOutAllSessions`

The module README remains the semantic authority for each complete thirteen-field use-case contract.

## Parameters

### Path

None.

### Query

None.

Query keys not declared here are rejected by the typed URL builder.

## Sources

- - `repository-app-router`: repository source `apps/web/src/app` — Support filesystem route materialization; delivery functions and module imports
