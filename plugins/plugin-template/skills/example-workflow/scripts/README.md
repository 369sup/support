# Skill scripts

Put deterministic helper scripts here. Document supported platforms, inputs,
outputs, side effects, dependencies, verification, and failure behavior in
`SKILL.md` before invoking them.
