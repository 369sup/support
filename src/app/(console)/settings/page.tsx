import { RoutePlaceholder } from "@/components/route-placeholder";

export default function SettingsPage() {
  return <RoutePlaceholder title="Settings" description="Configure workspace defaults, integrations, and access." />;
}
